import React from 'react';

import { Grid } from 'react-flexbox-grid/lib';

const DashboardPage = () => (
  <Grid fluid>
    <button>Liste products</button>
    <button>Nouveau product</button>
  </Grid>
);

export default DashboardPage;
